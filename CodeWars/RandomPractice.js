function divide(target, divisor) {
    let final = target % divisor;
        return final;
};

let final = divide(12, 3);
    if (final >= 1) {
        console.log('The remainder is ' + final)
    } else {
        console.log('No remainder')
    };